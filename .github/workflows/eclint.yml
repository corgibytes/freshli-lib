name: EditorConfig Lint

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  eclint:
    runs-on: ubuntu-latest
    steps:
      - name: ECLint
        uses: greut/eclint-action@v0.2.5
